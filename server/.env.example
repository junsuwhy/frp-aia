# ============================================
# frp + Caddy 環境變數配置範本
# ============================================
# 使用方式：
#   1. 複製此檔案為 .env
#   2. 修改下面的值
#   3. 確保 .env 在 .gitignore 中

# === 域名設定 ===
DOMAIN=test.yourdomain.com
EMAIL=your-email@example.com

# === 安全性設定 ===
# 使用以下指令生成安全的 token：
#   openssl rand -base64 32
FRP_TOKEN=change_this_to_random_string
FRP_WEB_USER=admin
FRP_WEB_PASSWORD=change_this_password

# === 網路設定 ===
FRP_BIND_PORT=7000
FRP_VHOST_HTTP_PORT=8080
FRP_VHOST_HTTPS_PORT=8443
FRP_WEB_PORT=7500

CADDY_HTTP_PORT=80
CADDY_HTTPS_PORT=443
CADDY_ADMIN_PORT=2019

# === 進階設定 ===
LOG_LEVEL=info
LOG_MAX_DAYS=7
TZ=Asia/Taipei

# === 安裝設定 ===
# 安裝檔案路徑（用於提供下載）
INSTALL_PATH=frp-install
# 允許下載的本地 IP（用逗號分隔多個 IP）
ALLOWED_IPS=127.0.0.1,::1,192.168.1.0/24
